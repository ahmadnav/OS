
// Constants
int charperline; // Characters per line, each char has an element after it used for color selection
int maxlines;	// 
char* vidmem;	//Pointer to the array which stores the letters to display on screen(vga) and there colors
//

void mscreen();
void clearscreen(); //Clear screens
void print(char* string);// Prints a string on to thte screen

